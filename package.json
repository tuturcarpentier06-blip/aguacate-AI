{
  "name": "aguacate-ai",
  "version": "3.5.0",
  "private": true,
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "multer": "^2.0.0",
    "openai": "^4.0.0"
  }
}
git add package.json pnpm-lock.yaml
git commit -m "chore: add node-cron"
git push origin main
